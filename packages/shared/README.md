# packages/shared

Shared TypeScript types used across `services/edge` and `apps/dashboard`: telemetry payload, enums, and `classifier_config.json` (provisional stress-level thresholds, vet-tunable).

See `docs/08 AI Classification Pipeline`.

_No shared code yet — scaffold only._
