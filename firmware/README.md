# firmware

- `esp32/` — device firmware (Arduino/PlatformIO).
- `simulator/` — payload simulator posting to `/telemetry`; stands in for hardware and is a first-class dev tool for all software sprints.

See `docs/06 IoT Wearable Device Design` and `docs/07 Sensor Data Pipeline`.

_No feature code yet — scaffold only._
